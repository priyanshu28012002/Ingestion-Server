#include <camerasSettings.hpp>




bool CamerasSettings::LoadCameras(const std::string &old_rtsp_path){

// Inplement the code to load the old rtsp path usnig mmmap
return true;
}

size_t CamerasSettings::getCameraCount(){
    return 0;
}